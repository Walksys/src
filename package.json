{
  "name": "discord-provision-backend",
  "version": "1.0.0",
  "description": "Simple backend to exchange Discord OAuth code and accept provisioning requests (demo).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.6.7",
    "dotenv": "^16.0.3"
  }
}